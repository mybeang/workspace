# Excute
```
# python ch_rst_table.py --help
Usage: ch_rst_table.py [OPTIONS]

Options:
  --filename TEXT     input the filename which want to change the table format
  --stringtable TEXT  input the string which want to change the table format
  --example           display the example
  --help              Show this message and exit.
```

# Example
```
# cat example.txt

# python ch_rst_table.py --failname
```
